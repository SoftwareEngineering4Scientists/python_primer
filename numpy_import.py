# import command can also use 'as' to provide an abbreviation for the package
import numpy as np

rand_integers = np.random.randint(0, 10, size=20)
print("Here are 20 random integers between 0 and 10:", rand_integers)
